#[strut::main]
async fn main() {
    // [x] async (tokio)
    // [x] external config
    // [x] graceful shutdown

    // + optionally:
    // [ ] logging / tracing
    // [ ] database (sqlx)
    // [ ] rabbitmq (lapin)
    // [ ] sentry
    // [ ] custom integrations
}
